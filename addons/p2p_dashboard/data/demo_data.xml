<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Demo data đã được comment để tránh tạo dữ liệu mẫu tự động -->
        <!-- 
        <record id="demo_p2p_dashboard" model="p2p.dashboard">
            <field name="name">Bảng điều khiển mẫu</field>
            <field name="date_from" eval="(datetime.now() - relativedelta(months=6)).strftime('%Y-%m-%d')"/>
            <field name="date_to" eval="datetime.now().strftime('%Y-%m-%d')"/>
        </record>
        -->
    </data>
</odoo>